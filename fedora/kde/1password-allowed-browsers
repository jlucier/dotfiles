# Copied to /etc/1password/custom_allowed_browsers by fedora/kde/install.sh.
# One executable file name per line. 1Password also requires the executable to
# be root-owned and not user-writable, which the /opt/zen install satisfies.
zen
zen-bin
